package dev.systemcrash.blade.cosmetics.mace;

public record MaceDefinition(
        String id,
        int cmd,
        String model,
        String name,
        String description,
        String color
) {
}
