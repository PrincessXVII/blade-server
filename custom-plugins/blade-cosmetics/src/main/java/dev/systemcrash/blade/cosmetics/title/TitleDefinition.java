package dev.systemcrash.blade.cosmetics.title;

public record TitleDefinition(
        String id,
        String display,
        String material,
        String description,
        String color,
        Integer price
) {
}
